@{
    ViewBag.Title = "Nouvelle tâche";

}

@*
Possède
    “Libellé”, “Description des opérations à effectuer” (multi lignes)
    Un identifiant, Un responsable
    Est associé à plusieurs exigences
Création
    Date début théorique d’execution
    Charge en nombre de jours
    Un tache précédente devant être terminé (au besoin)
Peut être réalisé par plusieurs exigences
Peut être
    Commencée (si date de démarrage réelle)
    Non commencée
    Terminée
*@

<h2>Ajoût d'une nouvelle tâche</h2>
<hr />
<form>
    <div class="form-group row">
        <label for="colFormLabel" class="col-sm-2 col-form-label">Nom de la tâche</label>
        <div class="col-sm-10">
            <input type="text" class="form-control" id="libelle" name="libelle" placeholder="Nom du projet" required>
        </div>
    </div>
    <div class="form-group row">
        <label for="colFormLabel" class="col-sm-2 col-form-label">Date de livraison prévue</label>
        <div class="col-sm-10">
            <input id="datelivraisonprevue" name="datelivraisonprevue" type="date" value="2018-07-12" required>
        </div>
        <label for="colFormLabel" class="col-sm-2 col-form-label">Date de livraison réelle</label>
        <div class="col-sm-10">
            <input id="datelivraisonprevue" name="datelivraisonreelle" type="date" value="2018-07-12" required>
        </div>
    </div>
    <div class="form-group row">
        <label for="colFormLabel" class="col-sm-2 col-form-label">Responsable</label>
        <div class="col-sm-10">
            <div class="dropdown">
                <select class="form-control" id="idresponsable" name="idresponsable" required>
                    @foreach (var utilisateur in ViewBag.Utilisateurs)
                    {
                        <option value="@utilisateur.ID">@utilisateur.Prenom @utilisateur.Nom</option>
                    }
                </select>
            </div>

        </div>
    </div>
    <div class="form-group row">
        <div class="col-sm-10">
            <button type="submit" class="btn btn-primary">Valider</button>
        </div>
    </div>
</form>